import { GENERATED_CANONICAL_PROJECT_SLUGS } from './generated/content';

export const CANONICAL_PROJECT_SLUGS = [...GENERATED_CANONICAL_PROJECT_SLUGS];

export const CANONICAL_PROJECT_SET = new Set(CANONICAL_PROJECT_SLUGS);
