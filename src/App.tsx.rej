@@ -659,16 +660,6 @@
             role={loadError ? 'alert' : 'progressbar'}
             aria-valuenow={loadError ? undefined : Math.round(loadProgress * 100)}
             aria-valuemin={loadError ? undefined : 0}
             aria-valuemax={loadError ? undefined : 100}
             aria-label={loadError ? 'Archive failed to load' : 'Loading archive'}
           >
-            {loadError ? (
-              <div className="bg-surface/95 backdrop-blur-xl border border-red-500/30 p-6 max-w-[400px] text-center shadow-2xl">
-                <span className="font-mono text-[10px] tracking-[0.2em] text-red-400 uppercase font-bold block mb-3">
-                  SYSTEM FAILURE
-                </span>
-                <p className="font-mono text-[9px] tracking-wider text-text-muted uppercase leading-relaxed">
-                  {loadError}
-                </p>
-              </div>
-            ) : (
